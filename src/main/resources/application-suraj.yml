spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: lightsuraj129@gmail.com
    password: pckc ttxg tyje vwqp
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: smtp.gmail.com
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/ams
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: ams
server:
  port: 8888